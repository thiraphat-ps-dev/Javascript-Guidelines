// [Types]

// [Primitive Type]

// Number
const number = 1;
const decimal = 0.25;

// String
var name = 'Thiraphat Putson';
const nickName = `Gun`;

// Boolean
const isShow = true;
const isHover = false;

// Null
const notHaveValue = null;
const noData = undefined;

// Date
const date = new Date();
const dateNow = Date.now();

// [Object Type]

// Array
const pages = [1, 2, 3, 4, 5];

// Object
const car = { name: 'Honda Civic', color: 'Red' };

// Function
function sumValue(x, y) {
  return x + y;
}
const summary = sumValue(2, 2);

// Or
const total = (x, y) => {
  return x + y;
};

// ---------------------------------------------------------------

// [Declaring]

const carName = 'Honda City'; // String

const pi = 3.14; // Number

const isTrue = true; // Boolean

const future = null; // null

const toDay = new Date(); // Date

const movie = { id: 1, title: 'star war', rating: 5 }; // object

const day = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun']; // Array

const sum = (x, y) => { return x + y; }; // Function

// Declare Variables in a Single Line
const firstName = 'Thiraphat', lastName = 'Putson', Age = 25;

// Declare a Variable without type
var one = 1;  // numeric value

one = 'one'; // string value

one= 1.1; // decimal value

one = true; // Boolean value

one = null; // null value

// ---------------------------------------------------------------
